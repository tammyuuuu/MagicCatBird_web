# 黑猫魔法项目协作规则

## 项目目标

本项目是一组相互连通的魔法主题网页：

- `reading_web.html`：黑猫占卜桌，包含牌盒、布袋、桌面交互和截图记录。
- `manifest_web.html`：显化之境，包含物品库和显化交互。
- `magic_web.html`：魔法工坊，远期重构为带可翻页魔法书的“炼药工艺”游戏。

当前阶段优先优化黑猫占卜桌与显化之境，用统一图片替换水晶、蜡烛等 Emoji 或简陋素材。除非用户另行安排，暂不大规模重构魔法工坊。

## 协作方式

当前任务担任项目总控。执行工作时，按需采用以下角色：美术设计、提示词、生图与素材、UI 开发、测试验收。岗位细则位于 `项目管理/部门/`。

每个需求必须：

1. 在 `项目管理/需求看板.md` 登记或更新状态。
2. 写清目标、涉及页面、验收标准和需要用户决定的事项。
3. 优先完成一个可审核的小样，再进行批量生产或大范围改动。
4. 将重要结论写入共享文档，不能只依赖聊天上下文。

## 用户审核关口

下列事项必须等待用户明确批准：

- 总体视觉方向或世界观发生变化。
- 从候选图中确定最终素材。
- 删除或替换用户已有的塔罗牌素材。
- 大幅改变核心玩法、数据结构或存档行为。
- 发布、上传或对外共享项目。

下列事项可在需求范围内自主完成：

- 阅读和分析现有代码。
- 拆分需求、编写提示词、制作候选方案。
- 对候选图进行命名、尺寸检查和归档。
- 集成用户已经批准的素材。
- 启动本地服务、浏览器验收和修复普通回归问题。

## 素材规则

- 新生成素材先放入 `图片/待审核/`。
- 用户明确批准后才能移入 `图片/已批准/`。
- UI 只能使用 `图片/已批准/` 中的新素材；现有历史素材不受此规则追溯影响。
- 不合格或被否决的素材移入 `图片/已弃用/`，除非用户要求，否则不要直接删除。
- 素材应优先保留高清透明母版，再按具体场景输出网页版本。
- 文件名使用稳定的英文小写命名，例如 `amethyst_master_v01.png`。
- 每次新增或更新素材，同时更新 `项目管理/素材清单.md`。

## 代码修改规则

- 修改前先了解相关 HTML、JavaScript 和现有交互，保留用户已有功能。
- 同一批改动尽量由一个 UI 实现角色统一写入，避免多人同时编辑同一 HTML 文件。
- 不得用未经批准的新图片直接替换正式页面素材。
- 修改后必须启动本地服务并在浏览器实际检查。
- 至少检查页面加载、图片资源、控制台错误、页面跳转和被修改的核心交互。
- 测试结果记录在相关工单中；未通过验收不得标记完成。

## 完成定义

一个需求只有同时满足以下条件才算完成：实现已经落盘、批准素材已正确归档、相关页面实际打开验证、没有由本次改动引入的明显错误、需求看板与工单状态已经更新。
